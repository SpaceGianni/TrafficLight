import fontawesome from '@fontawesome/fontawesome';
import faClock from '@fontawesome/fontawesome-free-solid/faCheck';

fontawesome.config = {
    autoReplaceSvg: 'nest'
}

fontawesome.library.add(faClock);